# Core inference
torch>=2.3.0
safetensors>=0.4.3

# Tokenizer only (no model download from transformers)
transformers>=4.45.0
huggingface_hub>=0.24.0

# API server
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0

# GUI inference backend
# macOS/Apple Silicon: CMAKE_ARGS="-DGGML_METAL=on" pip install "llama-cpp-python[server]"
# CUDA:               CMAKE_ARGS="-DGGML_CUDA=on"   pip install "llama-cpp-python[server]"
# CPU-only:           pip install "llama-cpp-python[server]"
llama-cpp-python[server]>=0.3.0
